# API Spec Sniffer

Sniffs undocumented API endpoints to save you from reading non-existent documentation. Run: `python api_sniffer.py <url>` with optional `--method` and `--data` flags. Works best when docs don't.